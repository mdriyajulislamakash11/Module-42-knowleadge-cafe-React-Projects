import React from "react";
import { FaBookmark } from "react-icons/fa";

const Blog = ({ blog, hanleBookark }) => {
 
  console.log(blog);

  return (
    <div>

    </div>
  );
};

export default Blog;
